# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: acainternal-secure-baseline
metadata:
  template: LZA-containerapps-internal-secure-baseline@0.0.1-beta
infra:
    provider: "bicep"
    path: "bicep"
    module: "main"
